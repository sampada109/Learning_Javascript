"use strict"; // this indicates the engine to treat each line of code as newer version of js

let name = "rohan"  //string
let age = 18  // number -> range = 2 to power 53
let isLogged = false  //boolean
let state;  //undefined
let city = null  //null = standalone value

// symbol = unique
// object

console.log(typeof undefined)   //undefined
console.log(typeof null)  //object